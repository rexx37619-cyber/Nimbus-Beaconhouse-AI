Nimbus final debugging brief:

Do not rewrite or redesign the working Nimbus UI. Fix only the two backend problems: Nano Banana 2 image generation and the private Puter agent.

PUBLIC VISUALS: Nimbus public users must not need a Puter login to generate a visual. Use the server-side GEMINI_API_KEY. Nano Banana 2 is Gemini model gemini-3.1-flash-image. Prefer the current Gemini REST Interactions endpoint /v1beta/interactions with response_format {type:"image", mime_type:"image/png", aspect_ratio:"16:9", image_size:"2K"} and parse output_image.data. If that fails, use the documented legacy Generate Content endpoint for gemini-3.1-flash-image with responseModalities ["TEXT","IMAGE"] and responseFormat.image {aspectRatio:"16:9", imageSize:"2K"}, parsing candidates[0].content.parts[].inlineData.data. A final compatibility fallback may try gemini-2.5-flash-image. Never call Puter image generation on the public site and never force a Puter login just to create an image. Keep the visual prompt realistic, professional, richly colored, presentation-quality, with meaningful subject imagery, icons/illustrations, depth, lighting, clear hierarchy, and concise labels. Avoid plain text-only charts, generic white boxes, and bare arrows.

PRIVATE WORKSPACE: Puter is required only for the private premium agent. After the user signs into Puter, discover the exact currently available model IDs with puter.ai.listModels('openai') and puter.ai.listModels('claude'). Select GPT-6 Astra by the returned ID, which is currently documented as openai/gpt-6-astra, and Claude Fable 5.1 by the returned ID, currently documented as anthropic/claude-fable-5-1. Do not hard-code an unqualified gpt-6-astra ID when a qualified ID is returned. The workspace dropdown should contain only Nimbus 5.7 Lor (GPT-6 Astra), Nimbus Fable 5.1 (Claude), and Nano Banana 2 (visuals). For chat, call puter.ai.chat with the exact discovered model ID and normalize:true. For Nano Banana 2, call the same /api/visual endpoint as the public site so the workspace agent does not depend on Puter image credits. The developer role is only an app-level context label; never claim code can override Puter's billing or allowance rules.

AUTH: Keep the existing two Puter usernames as the workspace allowlist: neat_ocean_262513 and peaceful_balloon_864250. Do not add other users. The sign-in button must call Puter authentication from a direct user click.

UI: Keep the existing working workspace buttons, previous chats, code blocks with language labels and Copy, and flowing three-dot waiting indicator with no connecting text. Never expose API keys. Never add GITHUB_TOKEN.
