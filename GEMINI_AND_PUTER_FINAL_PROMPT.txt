You are stabilizing Nimbus, a Vercel-hosted educational AI website. The local project is C:\Users\Hassan Rauf\Downloads\Nimbus_CLEAN and the GitHub repository is rexx37619-cyber/Nimbus-Beaconhouse-AI. Do not redesign the working interface. Two areas must be correct: (1) the private Puter workspace AI agent and controls, and (2) Nano Banana 2 visual generation.

REFERENCE BUILDS: Use the earlier Nimbus_5_7_Lor_IMAGE_INTERFACE_PUTER_ONLY build as the visual-card/UI reference because its public visual interface was the most stable. Use the earlier Nimbus_5_7_Lor_FINAL_DEV_WORKSPACE_VISUAL_FIX build as the workspace reference. Do not copy placeholder code from later Gemini drafts.

WORKSPACE: Puter login must happen from the visible sign-in button after Puter.js loads. Only these Puter usernames are permitted: neat_ocean_262513 and peaceful_balloon_864250. The workspace has working tabs, previous chats, New Chat, Clear History, model dropdown, Run, Revenue/PKR controls, File Editor Load/Refresh/Download, and UI Layout Apply/Export/Reset. All handlers must be initialized after DOMContentLoaded and guarded so one component error cannot disable the other controls.

MODEL AGENT: The workspace dropdown should contain only Nimbus 5.7 Lor • Ultra Modified, Nimbus Fable 5.1 • Ultra Modified, and Nano Banana 2 • Visuals. Do not treat Puter listModels results as strings. listModels returns objects with id/provider/name/aliases/cost metadata. Dynamically resolve GPT-6 Astra by id/name/alias containing gpt-6-astra and Claude Fable 5.1 by id/name/alias containing claude-fable-5-1. Use the exact returned model id in puter.ai.chat(). Use normalize:true and extract response.message.content. Do not incorrectly map Astra to gpt-4o. If Puter returns a low-balance/funding/allowance error, do not show the native error to the user. Silently fall back to the existing Nimbus /api/chat Gemini backend so the workspace agent remains usable. Do not claim that code can override Puter billing or make an account unlimited; the fallback exists to keep the agent functional.

VISUALS: Normal public Nimbus users must NOT need Puter login for image generation. Public and workspace visual requests should call /api/visual. Use Google Nano Banana 2 model gemini-2.5-flash-image with the current Gemini API. Prefer the current Interactions API endpoint https://generativelanguage.googleapis.com/v1beta/interactions with x-goog-api-key and response_format {type:'image', mime_type:'image/png', aspect_ratio:'16:9', image_size:'2K'}. Parse output_image.data or model_output step image blocks. Then use Generate Content as compatibility fallback, followed by gemini-2.5-flash-image compatibility fallback. Do not call Puter txt2img for public Nimbus visuals.

VISUAL QUALITY: The prompt must explicitly demand professional realistic educational visuals, realistic subject imagery, rich color, 3D/depth/lighting, accurate structure, polished textbook/poster quality, useful icons, concise readable labels, precise callouts/leader lines, strong composition, and 16:9 presentation quality. Prohibit generic three-box diagrams, simplistic arrow-only diagrams, plain text-only posters, empty placeholders, and UI wireframes. For science/anatomy use a realistic central cutaway or detailed illustration. For processes show real objects/stages and visual relationships. For code/game requests visualize architecture/mechanics/components, not long code. If Gemini image access is unavailable, use a dynamic topic-specific SVG fallback rather than a static generic template.

PUBLIC AI: Keep the fast Nimbus chat. Never show “Connecting to Nimbus”, provider diagnostics, stack traces, or raw API errors. Do not use ** bold markers or Markdown # headings in normal replies. For schoolwork provide keywords/facts/structure/flow rather than submission-ready prose; if the student asks to rewrite, say they must rephrase it themselves and provide only keywords/structure. Code must always be fenced with a language identifier.

VERIFY BEFORE DEPLOYING: Syntax-check every JavaScript file. Confirm every workspace HTML control id has a handler. Test Puter sign-in, allowed-user recognition, model switching, agent Run, low-balance fallback, visual generation, visual fallback, and each workspace tab/button. Do not leave console.log-only placeholder handlers. Do not reintroduce GITHUB_TOKEN or old Python/Render files.
