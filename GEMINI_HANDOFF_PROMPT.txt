You are taking over a Vercel-hosted educational AI site called Nimbus. Repository: https://github.com/rexx37619-cyber/Nimbus-Beaconhouse-AI. Local folder: C:\Users\Hassan Rauf\Downloads\Nimbus_CLEAN. Production: https://nimbus-beaconhouse-ai.vercel.app/ . Workspace: https://nimbus-beaconhouse-ai.vercel.app/workspace.html . Do not redesign the working UI unless necessary.

Nimbus has a polished student chat UI, model selector, recent chats, 24-hour usage display, decorated code blocks with language labels and Copy buttons, Beaconhouse/Nimbus branding, and a simple three-dot thinking indicator. Normal school answers should focus on keywords, facts, structure, concepts, diagrams and flowcharts rather than ready-to-submit prose. If a student asks for rewriting, tell them to rephrase it themselves and provide keywords/structure. Code must always use fenced Markdown with a real language identifier.

Final bugs to stabilize:
1) Workspace Puter sign-in/button wiring. The workspace must use the visible button and documented puter.auth.signIn() flow after Puter.js has loaded. Then call puter.auth.getUser(), normalize username, and allow exactly these two usernames: neat_ocean_262513 and peaceful_balloon_864250. Show ACCESS ALLOWED. Do not require a separate email permission flow. Initialize all workspace controls after DOMContentLoaded so one startup exception cannot stop later handlers. Working controls: Overview, AI Agent, Revenue, File Editor, UI Studio, New Chat, Clear History, Run, model selector, Load, Refresh, Download, Save, Apply, Export, Reset.

2) Workspace premium agent. Do not invent an unlimited/developer flag. Use Puter’s live model list to resolve the exact model IDs for GPT-6 Astra and Claude Fable 5.1, then call puter.ai.chat() with the exact returned ID. Keep the visible model choices small and Nimbus-branded. Premium chat uses Puter; normal public students do not.

3) Public visual generation. A known-good reference is the earlier Nimbus_5_7_Lor_IMAGE_INTERFACE_PUTER_ONLY build. Preserve its visual-card interface: NANO BANANA 2 • VISUAL, loading dots, then an actual image inside the card. Public students must NOT be asked to sign in to Puter for images. Call /api/visual server-side using GEMINI_API_KEY. Use Google’s documented Gemini image model gemini-3.1-flash-image (Nano Banana 2) with GenerateContent, responseModalities ['IMAGE'], responseFormat.image {aspectRatio:'16:9', imageSize:'2K'}, and parse candidates[0].content.parts[].inlineData. Try gemini-3.1-flash-image first, gemini-2.5-flash-image second for compatibility. If the Gemini key lacks image access/quota or the endpoint fails, do not show only a text error: return a polished local SVG infographic fallback so the student still sees a real visual card.

Visual prompts should ask for professional, colorful, realistic-looking educational posters/flowcharts with meaningful imagery, depth, lighting, clear hierarchy, arrows, icons and short labels; avoid plain white box diagrams and text-only visuals.

Use this project context to make a minimal, tested patch. Syntax-check all JS, verify the ZIP, and do not claim provider access is guaranteed if the API key lacks permission.