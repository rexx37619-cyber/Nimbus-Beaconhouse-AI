You are fixing an existing Vercel-hosted educational AI website called Nimbus. Use the GitHub repo rexx37619-cyber/Nimbus-Beaconhouse-AI and local project Nimbus_CLEAN as the source of truth. Do not redesign the UI. Preserve the existing working Nimbus visual-card interface and workspace appearance.

Known-good references: Nimbus_5_7_Lor_IMAGE_INTERFACE_PUTER_ONLY was the best public visual UI reference. Nimbus_5_7_Lor_FINAL_DEV_WORKSPACE_VISUAL_FIX was the best workspace reference. Do not reuse later drafts that contain placeholder console.log handlers or incorrect model mappings.

Workspace requirements: Puter sign-in is triggered by the visible button after Puter.js loads. Only usernames neat_ocean_262513 and peaceful_balloon_864250 are allowed. Initialize every control after DOMContentLoaded and protect each subsystem from one startup error disabling the rest. Tabs Overview, AI Agent, Revenue, File Editor and UI Studio must switch. New Chat, Clear History, Run, model dropdown, Load, Refresh, Download, Apply, Export and Reset must all have real working handlers.

Workspace AI: the dropdown contains only Nimbus 5.7 Lor • Ultra Modified, Nimbus Fable 5.1 • Ultra Modified, and Nano Banana 2 • Visuals. Use puter.ai.listModels() and treat results as objects with id/provider/name/aliases/cost. Dynamically resolve GPT-6 Astra by an id/name/alias containing gpt-6-astra and Fable 5.1 by claude-fable-5-1. Never incorrectly map GPT-6 Astra to GPT-4o. Pass the exact resolved Puter model id to puter.ai.chat(), use normalize:true, and read response.message.content. If Puter returns low-balance/funding/allowance/upgrade errors, silently fall back to the existing /api/chat Nimbus Gemini backend rather than showing a Puter billing error. Do not claim code can override Puter billing; the fallback exists only to keep the workspace agent usable.

Public image generation: never require Puter login for ordinary Nimbus users. All visual requests go to /api/visual using GEMINI_API_KEY. Gemini 2.5 Flash Image is gemini-2.5-flash-image. Prefer the current Gemini Interactions REST API at /v1beta/interactions with x-goog-api-key and response_format {type:'image', mime_type:'image/png', aspect_ratio:'16:9', image_size:'2K'}. Parse output_image.data or model_output step content blocks of type image. Use Generate Content as a compatibility fallback, then gemini-2.5-flash-image. Never call Puter txt2img from the public image path.

Image quality: make visuals professional, realistic, colorful, dimensional, polished and presentation-ready. For science/anatomy use a detailed central real or 3D cutaway subject with accurate structure, textures and leader callouts. For processes use real objects/stages and visual relationships, not generic boxes. Use short labels, icons and arrows only where useful. Prohibit plain text posters, empty placeholders, wireframes, generic three-box templates and simplistic arrow-only diagrams. For code/game help visualize architecture, mechanics, components and system flow.

Public AI style: fast; no “Connecting to Nimbus”; no ** bold markers; no unnecessary # headings; no provider diagnostics; schoolwork returns keywords, facts and structure rather than ready-to-submit prose; rewriting requests tell students they must rephrase it themselves; code is always in fenced blocks with language identifiers.

Verify before deploying: syntax-check every JS file; confirm every important workspace control has a real handler; test Puter sign-in, both allowed usernames, model selection, agent response, low-balance fallback, all workspace tabs/buttons, public /api/visual image generation, and the visual card renderer. Do not add GITHUB_TOKEN, old Python/Render files, or fake billing bypasses.
