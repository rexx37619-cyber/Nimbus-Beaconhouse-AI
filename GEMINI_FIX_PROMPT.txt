Nimbus Nano Banana 2 / Puter workspace debugging prompt

Fix Nimbus without rewriting working UI features. The public site uses Vercel serverless functions in /api and a GEMINI_API_KEY. Nano Banana 2 must use Google's current image-generation API for model gemini-3.1-flash-image. Prefer the current REST Interactions endpoint at /v1beta/interactions with response_format {type:"image", mime_type:"image/png", aspect_ratio:"16:9", image_size:"2K"}. Parse output_image.data. If that request is unavailable for the configured project, try the documented Generate Content endpoint as a compatibility fallback and parse candidates[0].content.parts[].inlineData.data. Never require Puter login for public Nimbus image generation. Keep image generation separate from premium chat models.

The private workspace uses Puter.js only for chat premium models. Discover models at runtime with puter.ai.listModels('openai') and puter.ai.listModels('claude') and use the exact returned model ID. Only expose GPT-6 Astra and Claude Fable 5.1 in the workspace dropdown, plus Nano Banana 2 as the visual option. GPT-6 Astra's documented Puter model ID is openai/gpt-6-astra; Claude Fable 5.1 is anthropic/claude-fable-5-1. The developer role can be described in the agent system prompt for context, but code must never claim it can override Puter's allowance or billing controls.

Keep the existing Nimbus UX: flowing three-dot wait indicator only, no connecting text, code blocks with language headers and Copy, previous private chats, working workspace buttons, 16:9 UI preview, and the existing public model selector. Never expose API keys in frontend code. Never put a GitHub token in the build. Preserve the keyword-focused schoolwork behavior and tell students to rephrase explanations themselves when they request rewriting.
